planning_task:
  description: >
    Analyze the user's query and create an execution plan:
    1. Identify the required data sources
    2. Determine the query execution order
    3. Identify possible optimization opportunities

    Query: {query}
  expected_output: >
    A clear query execution plan, including:
    1. Required data sources
    2. Query execution steps
    3. Expected output format
