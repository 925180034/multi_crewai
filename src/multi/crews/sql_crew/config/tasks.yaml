generate_sql:
  description: >
    Generate an accurate SQL query based on the provided information.
    Database Schema: {db_schema}
    Database ID: {db_id}
    Query: {query}
    Schema Mapping: {schema_matches}
  expected_output: >
    An accurate SQL query that correctly answers the user's question.
  agent: sql_generator_agent